# Portfolio Image Assets

Project screenshots are stored in `src/assets/images/projects/` and imported from `src/data/siteData.js`.

Current project screenshot files:

- `buffalo-river-ui-screenshot.png`
- `cmdd-ui-screenshot.png`
- `insura-match-ui.png`

To replace a screenshot later, keep the same filename or update the matching import in `src/data/siteData.js`.
