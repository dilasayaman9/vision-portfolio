# Portfolio Image Assets

Replace these placeholder SVG files with real project screenshots when they are ready.

Recommended screenshot size: 1440 x 900 pixels, or any image with a 16:10 aspect ratio.

Expected filenames:

- `placeholders/cmdd-website.svg`: appears on the CMDD project preview and case study.
- `placeholders/insuramatch-app.svg`: appears on the InsuraMatch project preview and case study.
- `placeholders/buffalo-river-getaway.svg`: appears on the Buffalo River Getaway project preview and case study.

To use real screenshots later, place the files in this folder or another folder inside `public/images`, then update the `image` value for each project in `src/data/siteData.js`.

Good replacement filenames:

- `cmdd-website.png`
- `insuramatch-app.png`
- `buffalo-river-getaway.png`

Use descriptive alt text in `src/data/siteData.js` once the real images are added.
